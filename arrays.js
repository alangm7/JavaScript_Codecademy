var bucketList = ['apple','carrot','cherry'];
bucketList.push('onion','peach'); //push, add element in array//
bucketList.pop(); //pop, delete last element in array//
console.log(bucketList.length);
console.log(bucketList);
