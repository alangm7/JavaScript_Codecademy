var makePizza = function (topping, crustType) {
  return 'The ' + crustType + ' crust pizza topped with ' + topping + ' is done. Let\'s eat!';
};

var customPizza = makePizza("bacon","pinapple");

console.log(customPizza);
